using System;
using System.Collections.Generic;
using System.Text;
using System.IO;
using EChestVC.Model;

namespace EChestVC.Directory.Load
{
    static class VersionDataFileManager
    {
        public static VersionData LoadVersionData(string dirpath, string filepath, VersionData.FileType filetype)
        {
            /*string path = Path.Combine(dirpath, filepath);
            using (FileStream fs = new FileStream(path, FileMode.Open))
            {
                StreamReader data = new StreamReader(fs);
            }*/
            throw new NotImplementedException();
        }
    }
}
